---
name: Sync Labels
on:
  pull_request

jobs:
  sync-labels:
    permissions:
      pull-requests: write
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Sync Labels
        uses: ./
        env:
          GH_TOKEN: ${{ github.token }}
